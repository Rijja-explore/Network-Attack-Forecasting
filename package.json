{
  "name": "network-attack-forecasting",
  "version": "1.0.0",
  "private": true,
  "description": "AI Based Network Attack Forecasting Platform",
  "scripts": {
    "dev": "npm --prefix dashboard run dev",
    "build": "npm --prefix dashboard run build",
    "preview": "npm --prefix dashboard run preview",
    "dashboard": "npm --prefix dashboard run dev",
    "server": "python -m uvicorn app.api:app --host 127.0.0.1 --port 8000",
    "start": "npm --prefix dashboard run dev"
  }
}
